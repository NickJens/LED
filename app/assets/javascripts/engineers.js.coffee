$ ->

# updateTokenEngineer = (renteeTokens, engineerTokens, numTokens) ->
#   if numTokens <= renteeTokens
#     renteeTokens = renteeTokens - numTokens
#     engineerTokens = engineerTokens + addedTokens
#   else
#     alert "Not enought tokens to distribute. Please purchase more"

#   renteeTokens = $("h4").data("renteetokens")
#   $(".added").click (event) ->
#     event.preventDefault()
#     engineerTokens = $(@).parent().parent().parent().data("token")
#     value = $(@).prev().val()
#     updateTokens()

# updateTokens = (renteeId, engineerId, numTokens) ->
#   if numTokens <= renteeTokens
#     renteeId = renteeTokens - numTokens
#     engineerId = engineerId + numTokens
#   else
#     "Not enought tokens to distribute. Please purchase more"




